<Page
    x:Class="WinUIClassicSamplesBrowser.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:WinUIClassicSamplesBrowser.ViewModels" 
    d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="256"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderThickness="2" BorderBrush="DarkOliveGreen" >
            <ListView x:Name="ExamplesListView" ItemsSource="{x:Bind SampleApps}" SelectionMode="Single" />
        </Border>
        <Border Grid.Column="1" BorderThickness="2" BorderBrush="DarkOliveGreen" >
            <TextBlock Text="Text inside a border" FontSize="18" />
        </Border>
    </Grid>
</Page>
